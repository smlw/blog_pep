const Post = require('./post');
const User = require('./user');
const Comment = require('./comment');
const Upload = require('./upload');
const Apc = require('./pwdSchema');

module.exports = {
    Post,
    User,
    Comment,
    Upload,
    Apc
}